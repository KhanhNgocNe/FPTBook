@model IEnumerable<FPTBook.Models.OrdersDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Books.bookName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ordersID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.amount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Books.bookName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ordersID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.amount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ordersdetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ordersdetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ordersdetailID })
        </td>
    </tr>
}

</table>
